# PostgreSQL — required for 6M scale on droplet
DATABASE_URL=postgresql://ppt_urls:pass@localhost:5432/ppt_urls

# SQLite — local dev only
# DATABASE_URL=sqlite:///data/pipeline.db

DATA_DIR=data
TARGET_COUNT=6000000

# Optional: production config (used on droplet)
# CONFIG_PATH=config/config.prod.yaml

# Optional logging
# LOG_LEVEL=INFO
# LOG_FILE=data/logs/pipeline.log
